# MongoFast
Basic project that implements mongo with docker

## Setup

- Add .env file
- Run command <br />
`
$ docker-compose build
`


## Run server
`
$ docker-compose up
`
<br />OR <br />
`
$ docker-compose up -d
